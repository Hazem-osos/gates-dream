'use client';

import PaginationDefault from '../Pagination';

/** Same pagination chrome as `@/components/Pagination`, with stable icons (no external CDN). */
export const Pagination = PaginationDefault;
