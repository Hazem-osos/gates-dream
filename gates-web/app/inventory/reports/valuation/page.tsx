import { redirect } from 'next/navigation';

/** Stub alias — full valuation filters pending API. */
export default function ValuationStubPage() {
  redirect('/inventory/reports/inventory-reports');
}
