import React from 'react';

export const metadata = {
  title: 'معاينة يومية نقاط البيع',
};

export default function PosDailyPreviewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
