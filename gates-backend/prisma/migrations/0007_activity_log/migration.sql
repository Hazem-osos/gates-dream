-- ActivityLog table setup (MySQL version)
-- Note: RLS is handled at application level for MySQL

-- Ensure activity_logs table exists (created by Prisma)
-- No RLS policies needed - handled by application middleware

SELECT 'ActivityLog RLS handled at application level for MySQL' AS note;
